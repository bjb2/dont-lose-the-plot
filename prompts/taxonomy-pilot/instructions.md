Project profile: {{PROFILE}}

Existing taxonomy:
<TAXONOMY>
{{TAXONOMY}}
</TAXONOMY>

Representative pilot segments selected from the beginning, middle, and end of the configured scope:
<SOURCE_SAMPLE>
{{SAMPLES}}
</SOURCE_SAMPLE>

Attempt a provisional extraction pass over every pilot segment before proposing taxonomy changes. Inventory the durable identities, recurring mechanisms, structured activities, embedded works, formal rules, transformations, social distinctions, relationships, and passage forms that a reader would need to revisit.

Then identify only the recurring structures that the existing taxonomy cannot express well. For every category proposal, explain why it deserves stable identity and pages rather than a tag or attribute, and cite exact evidence from at least two different pilot segments. If recommending a tag, attribute, or relationship, set `mergeInto` to the existing category that should own it; otherwise use `null`. Also propose the relationship predicates and passage kinds the full extraction will need. Use lowercase kebab-case IDs.

Before returning, check whether the proposed taxonomy would let the later full fan-out represent every provisional pilot record without forcing it into a misleading category. Do not force new categories merely to increase counts, but do not mistake the compact starter ontology for a completeness target.
